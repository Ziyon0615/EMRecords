services:
  - type: web
    name: profelect2-api
    env: node
    plan: free
    rootDir: ProfElect2-master
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATABASE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: ROBOFLOW_API_KEY
        sync: false
